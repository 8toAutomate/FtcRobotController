package org.firstinspires.ftc.teamcode.Sensors;

public class Rind_FInder {
}
    int RingsFound = 0;
    if (colorSensor instanceof DistanceSensor) {
        telemetry.addData("Distance (cm)", "%.3f", ((DistanceSensor) colorSensor).getDistance(DistanceUnit.CM));
    }
    window.print
    